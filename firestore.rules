rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default open for the dev preview environment as this is just a demo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
